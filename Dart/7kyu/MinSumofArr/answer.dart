void main() {
  List<int> arr = [9, 2, 8, 7, 5, 4, 0, 6];
  arr.sort();
  // int sum = 0;

  // for (var i = 0; i <= arr.length; i++) {
  //   int min = arr.first;
  //   int max = arr.last;
  //   sum += (min * max);
  //   arr.removeAt(0);
  //   arr.removeAt(arr.length - 1);
  // }
  print(arr);
}
